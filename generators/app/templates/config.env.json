{
 <% if (includeMongo) { %>,
 "data": {<% for (var resource in resources) { %>
   "<%= resource.name %>": {
     uri: 'mongodb://localhost:27017/<%= resource.name %>'
   }<% } %>
 },<% } %>
 "service": {
  "port": 3000
 }
}